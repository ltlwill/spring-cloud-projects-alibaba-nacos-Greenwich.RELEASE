spring: 
  # sleuth   
  sleuth:
    web:
      enabled: true
    sampler:
      probability: 1.0
management:
  endpoints:
    web:
      exposure:
        include: "*"  
zuul:
  host:
    connect-timeout-millis: 30000  # 30秒超时
    connection-request-timeout-millis: 30000
    socket-timeout-millis: 30000        
ribbon: 
  ReadTimeout: 30000
  ConnectTimeout: 30000		
commonConfig: this is a common config aa xx